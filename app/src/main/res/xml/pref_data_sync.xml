<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:androi="http://schemas.android.com/apk/res-auto">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->


    <!-- This preference simply launches an intent when selected. Use this UI sparingly, per
         design guidelines. -->

    <MultiSelectListPreference
        android:title="@string/pref_fav_colour"
        android:entryValues="@array/colours_text"
        android:entries="@array/colours_text"

    />

    <CheckBoxPreference
        android:key="show_wishlist_item_in_compare"
        android:title="@string/show_wishlist_item_in_compare"
        android:defaultValue="true"
        />


</PreferenceScreen>
